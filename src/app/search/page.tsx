import SearchInterface from '@/components/SearchInterface'

export default function SearchPage() {
  return (
    <main className="min-h-screen bg-gradient-to-br from-pink-50 via-green-50 to-blue-50">
      <SearchInterface />
    </main>
  )
}
